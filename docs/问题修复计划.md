# 餐厅点餐系统 - 问题修复计划文档（优化版）

> 创建时间：2026-02-26  
> 更新时间：2026-02-26（根据新状态逻辑优化）  
> 关联问题：Pad订单管理功能优化

---

## 📋 问题清单总览

| 编号 | 问题描述 | 严重程度 | 修复优先级 |
|------|---------|---------|-----------|
| 1 | 订单详情页无法显示已点菜品 | 🔴 高 | P0 |
| 2 | 加菜应汇总到同一订单 | 🔴 高 | P0 |
| 3 | 状态流转逻辑错误 | 🔴 高 | P0 |
| 4 | 结账逻辑复杂场景支持 | 🔴 高 | P0 |

---

## 🔄 新状态流转设计（优化版）

### 状态定义

| 状态码 | 状态名 | 说明 |
|--------|--------|------|
| 0 | 待上菜 | 订单已创建，等待上菜 |
| 1 | 上菜中 | 有菜品正在上菜/部分已上 |
| 2 | 待结账 | 所有菜品已上齐，等待结账 |
| 3 | 已完成 | 已结账，订单完成 |
| 4 | 追加订单 | 已结账后又加菜（特殊状态） |

### 状态流转规则

```
┌─────────────────────────────────────────────────────────────┐
│                        状态流转图                            │
└─────────────────────────────────────────────────────────────┘

【新建订单】
     ↓
┌─────────────┐         有菜品在制作/完成          ┌─────────────┐
│  待上菜(0)   │ ─────────────────────────────────▶│  上菜中(1)   │
│ (默认状态)   │                                    │             │
└──────┬──────┘                                    └──────┬──────┘
       │                                                  │
       │ 所有菜品制作完成                                  │ 所有菜品上齐
       │                                                  │
       ▼                                                  ▼
┌─────────────┐                                    ┌─────────────┐
│  待结账(2)   │◀───────────────────────────────────│  （自动流转）│
│             │    上菜中且所有菜品完成               │             │
└──────┬──────┘                                    └─────────────┘
       │
       │ 点击结账
       ▼
┌─────────────┐
│  已完成(3)   │
└──────┬──────┘
       │
       │ 追加菜品（已结账后加菜）
       ▼
┌─────────────┐
│ 追加订单(4)  │────▶ 回到【待上菜】或【上菜中】
│ （子订单）   │      新菜品走正常流程
└─────────────┘
```

### 状态切换条件详解

#### 1. 待上菜(0) → 上菜中(1)
**触发条件：**
- 任一菜品标记为"制作中" 或 "已完成"

**实现逻辑：**
```java
if (orderStatus == 0 && hasItemInCookingOrFinished()) {
    order.setStatus(1); // 上菜中
}
```

#### 2. 上菜中(1) → 待结账(2)
**触发条件：**
- 所有菜品都标记为"已完成"

**实现逻辑：**
```java
if (orderStatus == 1 && allItemsFinished()) {
    order.setStatus(2); // 待结账
}
```

#### 3. 待结账(2) → 已完成(3)
**触发条件：**
- 点击结账按钮，确认收款

**实现逻辑：**
```java
if (orderStatus == 2) {
    order.setStatus(3); // 已完成
    order.setPayTime(now);
    order.setPayType(payType);
}
```

#### 4. 已完成(3) → 追加订单(4)
**触发条件：**
- 已结账后，再次点击"加菜"

**实现逻辑：**
```java
// 创建"追加订单"记录，关联原订单
// 或者使用子订单模式
```

---

## 💰 结账金额计算逻辑

### 场景1：正常结账（所有菜品未结账）
```
应收金额 = 所有菜品金额总和
```

### 场景2：已结账后加菜
```
原订单：已结账 ¥200
加菜：¥50

显示：
- 原订单已付：¥200 ✓
- 新增菜品：¥50
- 本次应收：¥50
```

### 实现方案

**方案A：子订单模式（推荐）**
```
主订单：ORD001（已完成，¥200）
  └─ 子订单：ORD001-ADD（待结账，¥50）

结账时分别处理
```

**方案B：标记已结账商品模式**
```
每个菜品项增加 isPaid 字段
结账时只计算 isPaid=false 的菜品
```

---

## 🔧 问题修复详情

### 问题1：订单详情页无法显示已点菜品

#### 根因分析
API返回正确，但前端可能没有正确渲染items数组

#### 修复步骤
1. 检查 OrderDetail.vue 中 items 的初始化
2. 添加调试日志确认数据
3. 修复模板渲染

#### 代码修复
```vue
<!-- OrderDetail.vue -->
<div class="items-list" v-if="items && items.length > 0">
  <div 
    v-for="item in items" 
    :key="item.id"
    class="item-row"
  >
    <div class="item-name">{{ item.dishName }}</div>
    <div class="item-quantity">x{{ item.quantity }}</div>
    <div class="item-price">¥{{ item.subtotal?.toFixed(2) }}</div>
    <div class="item-status">
      <!-- 状态切换按钮 -->
    </div>
  </div>
</div>

<el-empty v-else description="暂无菜品" />
```

---

### 问题2：加菜汇总到同一订单

#### 根因分析
每次提交都调用 createOrder，没有检测现有订单

#### 修复方案
**智能加菜模式**

```typescript
// OrderPad.vue
const submitOrder = async () => {
  if (cartItems.length === 0) {
    ElMessage.warning('购物车为空')
    return
  }
  
  // 检测模式
  const mode = route.query.mode
  const existingOrderId = route.query.orderId
  
  if (mode === 'add' && existingOrderId) {
    // 加菜模式：添加到现有订单
    await addDishToOrder(existingOrderId, {
      items: cartItems
    })
    ElMessage.success('加菜成功')
  } else {
    // 新建订单
    const order = await createOrder({
      tableId: tableId.value,
      customerCount: customerCount.value,
      cartItems: cartItems
    })
    ElMessage.success('下单成功')
    
    // 保存订单ID到query，方便后续加菜
    router.replace({
      query: { 
        ...route.query,
        orderId: order.id 
      }
    })
  }
  
  // 清空购物车
  cartStore.clearCart()
}
```

---

### 问题3 & 4：状态流转和结账逻辑

#### 后端修改

**OrderStatusService.java（新增）**
```java
@Service
@RequiredArgsConstructor
public class OrderStatusService {
    
    private final OrderMapper orderMapper;
    private final OrderItemMapper orderItemMapper;
    
    /**
     * 根据菜品状态自动更新订单状态
     */
    public void updateOrderStatus(Long orderId) {
        Order order = orderMapper.selectById(orderId);
        List<OrderItem> items = orderItemMapper.selectByOrderId(orderId);
        
        int currentStatus = order.getStatus();
        
        // 已完成或追加订单状态不再自动变更
        if (currentStatus >= 3) {
            return;
        }
        
        boolean hasCooking = items.stream().anyMatch(i -> i.getStatus() == 1);
        boolean hasFinished = items.stream().anyMatch(i -> i.getStatus() == 2);
        boolean allFinished = items.stream().allMatch(i -> i.getStatus() == 2);
        
        if (currentStatus == 0) {
            // 待上菜状态
            if (hasCooking || hasFinished) {
                order.setStatus(1); // 转为上菜中
                orderMapper.updateById(order);
            }
        } else if (currentStatus == 1) {
            // 上菜中状态
            if (allFinished) {
                order.setStatus(2); // 转为待结账
                orderMapper.updateById(order);
            }
        }
    }
    
    /**
     * 结账
     */
    @Transactional
    public void checkout(Long orderId, Integer payType, BigDecimal amount) {
        Order order = orderMapper.selectById(orderId);
        
        // 计算未结账金额
        List<OrderItem> unpaidItems = getUnpaidItems(orderId);
        BigDecimal shouldPay = unpaidItems.stream()
            .map(OrderItem::getSubtotal)
            .reduce(BigDecimal.ZERO, BigDecimal::add);
        
        // 标记已结账
        for (OrderItem item : unpaidItems) {
            item.setIsPaid(1);
            orderItemMapper.updateById(item);
        }
        
        // 更新订单
        order.setPayAmount(order.getPayAmount().add(amount));
        order.setPayType(payType);
        
        // 检查是否还有未结账菜品
        if (getUnpaidItems(orderId).isEmpty()) {
            order.setStatus(3); // 已完成
            order.setPayTime(LocalDateTime.now());
        } else {
            order.setStatus(4); // 追加订单（部分结账）
        }
        
        orderMapper.updateById(order);
    }
    
    /**
     * 获取未结账的菜品
     */
    public List<OrderItem> getUnpaidItems(Long orderId) {
        LambdaQueryWrapper<OrderItem> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(OrderItem::getOrderId, orderId)
               .eq(OrderItem::getIsPaid, 0);
        return orderItemMapper.selectList(wrapper);
    }
}
```

**数据库修改**
```sql
-- 给 order_item 添加是否已结账字段
ALTER TABLE order_item ADD COLUMN is_paid TINYINT DEFAULT 0 COMMENT '是否已结账：0否 1是';
```

#### 前端修改

**OrderDetail.vue 按钮显示逻辑**
```typescript
// 始终显示结账按钮
const showCheckout = computed(() => {
  return order.value?.status !== 3 // 非已完成状态都显示
})

// 结账时计算金额
const checkoutAmount = computed(() => {
  // 只计算未结账的菜品
  return items.value
    .filter(item => !item.isPaid)
    .reduce((sum, item) => sum + item.subtotal, 0)
})

// 显示已结账标识
const isItemPaid = (item) => item.isPaid === 1
```

---

## 📱 特殊场景处理

### 场景1：提前结账
```
流程：
1. 点完菜（待上菜/上菜中）
2. 顾客要求提前结账
3. 点击结账 → 支付当前所有菜品
4. 订单标记为"已结账"
5. 但状态保持"上菜中"（继续上菜）
6. 所有菜品上齐后 → 自动转为"已完成"
```

### 场景2：结账后加菜
```
流程：
1. 原订单已完成结账
2. 点击"加菜" → 进入点餐页面
3. 选择菜品 → 提交
4. 系统检测：已结账订单
5. 新增菜品标记为"未结账"
6. 订单状态转为"追加订单"
7. 显示"本次应收"（只含新菜品）
8. 结账后 → 转为"已完成"
```

---

## ✅ 验收标准

### 基础功能
- [ ] 订单详情页显示所有已点菜品
- [ ] 同一桌台多次加菜汇总到一个订单
- [ ] 状态根据菜品自动流转

### 结账功能
- [ ] 随时可点击结账按钮
- [ ] 只计算未结账的菜品
- [ ] 已结账菜品标记清晰
- [ ] 结账后加菜正确计算

### 边界场景
- [ ] 提前结账后继续上菜正常
- [ ] 结账后加菜流程正确
- [ ] 多次加菜多次结账累计正确

---

## 🚀 修复计划

### 阶段1：数据库和基础API（2小时）
- [ ] 添加 order_item.is_paid 字段
- [ ] 创建 OrderStatusService
- [ ] 修改状态更新逻辑
- [ ] 修改结账逻辑

### 阶段2：后端接口（2小时）
- [ ] 修改 updateItemStatus（自动更新订单状态）
- [ ] 修改 payOrder（支持部分结账）
- [ ] 添加 getUnpaidAmount 接口

### 阶段3：前端修复（3小时）
- [ ] 修复 OrderDetail.vue 菜品显示
- [ ] 修复 OrderPad.vue 加菜逻辑
- [ ] 更新结账按钮显示逻辑
- [ ] 更新状态显示

### 阶段4：测试验证（2小时）
- [ ] 基础流程测试
- [ ] 加菜结账测试
- [ ] 提前结账测试
- [ ] 边界场景测试

---

**总计预估：约9小时**

---

## 📝 开始修复

确认计划后，按阶段执行：
1. **第一阶段**：数据库 + 后端基础
2. **第二阶段**：后端接口优化  
3. **第三阶段**：前端修复
4. **第四阶段**：全面测试

每完成一个阶段向您汇报！
